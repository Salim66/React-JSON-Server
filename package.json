{
  "name": "eshop",
  "version": "1.0.0",
  "description": "this is restApi database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server-start": "json-server --watch db.json --port 5050"
  },
  "author": "salim hasan",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
